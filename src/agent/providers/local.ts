// Placeholder for a future local-model provider variant if needed.
// For now, the openai-compatible provider handles Ollama directly.
// This file is reserved for a provider that communicates with a model
// via a non-HTTP interface (e.g. native bindings, stdin/stdout).

export {};
