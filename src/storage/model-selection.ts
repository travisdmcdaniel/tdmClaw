// Model selection persistence is handled via the SettingsStore (storage/settings.ts).
// Keys used:
//   model.active    — name of the currently selected model
//   model.fallbacks — JSON array of fallback model names in priority order
//
// This module is a placeholder for any future model-selection-specific query helpers.

export {};
